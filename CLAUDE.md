<!-- SPECKIT START -->
Active feature: Technical-Writing Canvas — syntax highlighting, richer code, callouts/footnotes & renderer tests (`004-technical-writing-canvas`) — IMPLEMENTED.
Plan: `specs/004-technical-writing-canvas/plan.md` (read first for the approach + Constitution Check + the Principle III "sanctioned body-content styling" reconciliation; contracts in `specs/004-technical-writing-canvas/contracts/{highlighter,code-block,callout-footnote,renderer-tests,verifier}.md`). Prior features: `specs/003-positioning-geo-performance/plan.md` (positioning + heading anchors + portfolio fonts), `specs/002-seo-geo-publish/plan.md` (SEO/GEO + one-commit publishing), `specs/001-obsidian-blog/plan.md` (the blog generator itself).
Constitution: `.specify/memory/constitution.md` (**v1.4.0**, 8 non-negotiable principles — SEO-static generation, GitHub Pages only, design fidelity (**III now permits a bounded "sanctioned body-content semantic styling" exception: new `blog.css` classes for code-token highlighting/callouts/footnotes/line-emphasis/quote-table refinements, the design's existing palette+fonts only, body-content only, portfolio/chrome untouched**), Obsidian single source, per-page SEO/GEO, a11y+CWV, non-destructive to portfolio (two bounded zones: `LATEST-NOTES` + `PORTFOLIO-FONTS`), and VIII machine-readable discovery & single-source publishing).
Stack: Python 3.11+ static generator (PyYAML + an in-house, stdlib-only Markdown renderer — no Markdown library, no syntax-highlighting library) rendering Obsidian markdown into design templates extracted from the bundled blog design; deploys to GitHub Pages via Actions. Author surface: `content/blog/`. Design source: `templates/blog/`. Generator: `scripts/blog/` (incl. `feed.py` Atom + `llms.py`; 003: grounded identity in `seo.py`+`config.py`, heading anchors in `markdown_render.py`; **004: `highlight.py` NEW — vendored 10-language syntax highlighter; `markdown_render.py` adds info-string parsing + filename/line-emphasis, Obsidian `> [!kind]` callouts, `[^id]` footnotes, hardened GFM tables**). Renderer tests: `tests/` (stdlib `unittest`, 71 tests; `python -m unittest discover -s tests`; CI runs them before build). Build `scripts/build_blog.py`, verify `scripts/verify_build.py` (DoD gate, **318 checks**). Never edit generated HTML; the portfolio `index.html` is touched only inside its `<!--LATEST-NOTES:*-->` and `<!--PORTFOLIO-FONTS:*-->` markers; new visual styling lives ONLY in `templates/blog/assets/blog.css` under `#blog-root`.
<!-- SPECKIT END -->
