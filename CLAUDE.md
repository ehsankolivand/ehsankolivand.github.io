<!-- SPECKIT START -->
Active feature: Android-Engineer Positioning, Deep-Citability & Portfolio Performance (`003-positioning-geo-performance`) — IMPLEMENTED.
Plan: `specs/003-positioning-geo-performance/plan.md` (read first for the approach + Constitution Check + font-fidelity reconciliation; contracts in `specs/003-positioning-geo-performance/contracts/`). Prior features: `specs/002-seo-geo-publish/plan.md` (SEO/GEO + one-commit publishing), `specs/001-obsidian-blog/plan.md` (the blog generator itself).
Constitution: `.specify/memory/constitution.md` (**v1.3.0**, 8 non-negotiable principles — SEO-static generation, GitHub Pages only, design fidelity, Obsidian single source, per-page SEO/GEO, a11y+CWV, non-destructive to portfolio (**now two bounded zones: `LATEST-NOTES` + the `PORTFOLIO-FONTS` fidelity-proven font-data zone**), and VIII machine-readable discovery & single-source publishing).
Stack: Python 3.11+ static generator (PyYAML + an in-house, stdlib-only Markdown renderer — no Markdown library) rendering Obsidian markdown into design templates extracted from the bundled blog design; deploys to GitHub Pages via Actions. Author surface: `content/blog/`. Design source: `templates/blog/`. Generator: `scripts/blog/` (incl. `feed.py` Atom + `llms.py`; 003: grounded `knowsAbout`/`jobTitle` identity in `seo.py`+`config.py`, deterministic heading anchors in `markdown_render.py`). Build `scripts/build_blog.py`, verify `scripts/verify_build.py` (DoD gate, **273 checks**). Never edit generated HTML; the portfolio `index.html` is touched only inside its `<!--LATEST-NOTES:*-->` and `<!--PORTFOLIO-FONTS:*-->` markers (the latter a one-time, fidelity-proven, non-visual font subset; recoverable baseline at `assets/portfolio-fonts/index.baseline.html`).
<!-- SPECKIT END -->
